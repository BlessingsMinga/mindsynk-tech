# SQLite database used by Prisma in local development.
DATABASE_URL="file:./db/custom.db"

# HTTPS webhook for contact-form deliveries. Configure this with your email,
# CRM, or automation provider; the form returns a 503 response when it is unset.
CONTACT_WEBHOOK_URL="https://example.com/contact-webhook"
